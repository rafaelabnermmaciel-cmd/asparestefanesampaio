// Configuração do Firebase deste app — cole aqui os valores do seu projeto (veja SETUP.md).
//
// A "firebaseConfig" abaixo NÃO é secreta — ela é feita pra ficar visível no código do site
// (é assim que qualquer app que usa Firebase direto do navegador funciona). Quem protege os
// dados são as regras de segurança do Firestore (Cloud Firestore → Regras no console), não o
// sigilo destes valores.
export const firebaseConfig = {
  apiKey: 'COLE_AQUI_A_API_KEY',
  authDomain: 'COLE_AQUI.firebaseapp.com',
  projectId: 'COLE_AQUI_O_PROJECT_ID',
  storageBucket: 'COLE_AQUI.appspot.com',
  messagingSenderId: 'COLE_AQUI',
  appId: 'COLE_AQUI',
};
